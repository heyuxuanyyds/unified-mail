{
  "name": "unified-mail",
  "version": "1.0.0",
  "description": "Unified mail manager on Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "edgeport": "^0.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
